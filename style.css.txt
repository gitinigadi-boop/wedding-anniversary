*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:'Poppins',sans-serif;
}

body{
    background:linear-gradient(135deg,#000000,#2b0000,#4d0000);
    min-height:100vh;
    display:flex;
    justify-content:center;
    align-items:center;
    text-align:center;
    overflow:hidden;
    color:white;
}

.overlay{
    width:90%;
    max-width:600px;
    padding:25px;
    background:rgba(0,0,0,0.55);
    border:3px solid gold;
    border-radius:20px;
    box-shadow:0 0 30px gold;
}

.title{
    font-family:'Cinzel',serif;
    font-size:40px;
    color:gold;
    text-shadow:0 0 20px gold;
    animation:glow 2s infinite alternate;
}

h2{
    margin-top:15px;
    color:#fff;
    font-size:28px;
}

.slideshow{
    margin:25px auto;
}

.slideshow img{
    width:280px;
    height:350px;
    object-fit:cover;
    border-radius:20px;
    border:5px solid gold;
    box-shadow:0 0 25px gold;
    animation:zoom 6s infinite;
}

.wish{
    margin-top:20px;
    font-size:18px;
    line-height:30px;
}

button{
    margin-top:25px;
    padding:12px 28px;
    border:none;
    border-radius:30px;
    background:gold;
    color:black;
    font-size:18px;
    cursor:pointer;
    font-weight:bold;
}

button:hover{
    background:white;
}

@keyframes glow{
    from{
        text-shadow:0 0 10px gold;
    }
    to{
        text-shadow:0 0 35px gold;
    }
}

@keyframes zoom{
    0%{
        transform:scale(1);
    }
    50%{
        transform:scale(1.05);
    }
    100%{
        transform:scale(1);
    }
}